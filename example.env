# WordPress Configuration (optional)
# Leave blank to use MOCK MODE, or set in the app: Profile (admin) > WordPress integration.
WP_API_URL=https://your-domain.com/wp-json/wp/v2/media
WP_USER=your_username
WP_PASS=your_application_password_here

# Flask Configuration
FLASK_SECRET_KEY=generate-a-random-string-here

# Optional: Redis Cache (e.g., redis://localhost:6379/0)
REDIS_URL=

# Optional: Redis for rate limits (production; e.g. redis://localhost:6379/1)
# RATELIMIT_STORAGE_URL=

# Optional: App Password for the email sender
# EMAIL_APP_PASSWORD=

# AMT (Authentication, Authorization, Multi-Tenancy)
ENABLE_REGISTRATION=0
API_TOKEN_EXPIRY_DAYS=90
# For migration: creates default admin when set
# ADMIN_USERNAME=admin
# ADMIN_EMAIL=admin@localhost
# ADMIN_PASSWORD=secret
